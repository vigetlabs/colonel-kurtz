/* @flow */

var KeyMirror = require('react/lib/keyMirror')

var BlockConstants = KeyMirror({
  BLOCK_CREATE  : null,
  BLOCK_UPDATE  : null,
  BLOCK_DESTROY : null
})

module.exports = BlockConstants
